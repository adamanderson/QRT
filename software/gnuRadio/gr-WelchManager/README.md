This is the current Welch module, ready to be tested on the Raspberry Pi.  It should be usable in GNURadio Companion.  To install:
$mkdir build
$cd build
$cmake ../
$make
$make test
$sudo make install
